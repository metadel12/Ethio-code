services:
  backend:
    build: ./backend
    restart: unless-stopped

  nginx:
    image: nginx:stable
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
